# 📋 Пошаговая инструкция по запуску

## Часть 1: Прошивка STM32 (NUCLEO-F103RB)

### Вариант A: Используя STM32CubeIDE (рекомендуется)

1. **Откройте проект:**
   - Установите STM32CubeIDE (если еще не установлена)
   - File → Open Projects from File System
   - Выберите папку `D:\7semestr\paint`
   - Нажмите Finish

2. **Проверьте конфигурацию (опционально):**
   - Откройте файл `paint.ioc` двойным кликом
   - Убедитесь, что:
     - ADC1 настроен на PA0 (Channel 0)
     - ADC2 настроен на PA1 (Channel 1)
     - USART2 настроен на 115200 бод (PA2/PA3)
     - GPIO настроены согласно таблице в README
   - Если что-то не так - исправьте и нажмите "Generate Code"

3. **Скомпилируйте проект:**
   - Проект → Build All (или Ctrl+B)
   - Дождитесь завершения компиляции

4. **Подключите NUCLEO-F103RB:**
   - Подключите плату к компьютеру через USB
   - Убедитесь, что JoystickShield установлен на плату

5. **Загрузите прошивку:**
   - Проект → Run → Run (или Ctrl+F11)
   - Или нажмите кнопку "Run" на панели инструментов
   - Прошивка загрузится автоматически

### Вариант B: Используя Keil MDK-ARM (если проект для Keil)

1. **Откройте проект:**
   - Откройте файл `MDK-ARM\paint.uvprojx` в Keil MDK-ARM

2. **Скомпилируйте:**
   - Project → Build Target (или F7)

3. **Загрузите:**
   - Flash → Download (или F8)

## Часть 2: Python приложение

### Шаг 1: Установка Python

Если Python не установлен:
- Скачайте Python 3.8+ с [python.org](https://www.python.org/downloads/)
- При установке отметьте "Add Python to PATH"

### Шаг 2: Установка зависимостей

Откройте терминал (PowerShell или Command Prompt) в папке проекта:

```bash
cd D:\7semestr\paint
pip install -r requirements.txt
```

Если pip не найден, попробуйте:
```bash
python -m pip install -r requirements.txt
```

### Шаг 3: Определение COM порта

**Windows:**
1. Откройте Диспетчер устройств (Win+X → Диспетчер устройств)
2. Разверните "Порты (COM и LPT)"
3. Найдите "STMicroelectronics Virtual COM Port" или "STM32 Virtual COM Port"
4. Запомните номер COM порта (например, COM3, COM4, COM5...)

**Альтернативный способ (PowerShell):**
```powershell
Get-PnPDevice -Class Ports | Where-Object {$_.FriendlyName -like "*STM*"}
```

### Шаг 4: Настройка COM порта в скрипте

Откройте файл `paint_receiver.py` в любом текстовом редакторе и найдите строку:

```python
COM_PORT = 'COM3'  # Измените на ваш COM порт
```

Замените `'COM3'` на ваш COM порт (например, `'COM4'`, `'COM5'` и т.д.)

### Шаг 5: Запуск приложения

В терминале выполните:

```bash
python paint_receiver.py
```

Или двойным кликом по файлу `paint_receiver.py` (если Python ассоциирован с .py файлами)

## Что должно произойти:

1. ✅ В терминале появится сообщение: `Подключено к COMX на 115200 бод`
2. ✅ Откроется окно Pygame с белым экраном
3. ✅ В центре экрана появится красный курсор
4. ✅ При движении джойстика курсор должен двигаться

## Проверка работы:

1. **Джойстик:**
   - Двигайте джойстик в разные стороны
   - Курсор должен двигаться по экрану

2. **Кнопка E (удержание):**
   - Удерживайте кнопку E
   - Двигайте джойстик
   - Должна появляться линия

3. **Кнопка A:**
   - Нажмите кнопку A
   - В левом верхнем углу должен измениться номер цвета

4. **Кнопка C:**
   - Нажмите кнопку C
   - Экран должен очиститься

## Возможные проблемы и решения:

### ❌ "Ошибка подключения к COM3"
**Решение:**
- Проверьте, что NUCLEO подключен к компьютеру
- Проверьте правильность COM порта в скрипте
- Закройте другие программы, использующие COM порт (терминалы, Arduino IDE и т.д.)
- Попробуйте переподключить USB кабель

### ❌ "ModuleNotFoundError: No module named 'pygame'"
**Решение:**
```bash
pip install pygame pyserial
```

### ❌ "ModuleNotFoundError: No module named 'serial'"
**Решение:**
```bash
pip install pyserial
```

### ❌ Курсор не двигается
**Решение:**
- Проверьте, что данные приходят (можно открыть монитор порта на 115200 бод)
- Проверьте, что джойстик правильно подключен
- Попробуйте перезапустить Python скрипт

### ❌ Кнопки не работают
**Решение:**
- Убедитесь, что прошивка загружена в микроконтроллер
- Проверьте конфигурацию EXTI в CubeMX
- Перезагрузите NUCLEO (кнопка Reset)

### ❌ Джойстик двигается в неправильную сторону
**Решение:**
- Откройте `paint_receiver.py`
- Найдите функции `normalize_joystick_x` и `normalize_joystick_y`
- Поменяйте местами знаки + и - для нужной оси

## Быстрая проверка UART (опционально):

Можно использовать монитор порта для проверки данных:

1. Откройте PuTTY, Tera Term или Serial Monitor
2. Настройте:
   - COM порт: ваш COM порт
   - Скорость: 115200
   - Data bits: 8
   - Stop bits: 1
   - Parity: None
3. Подключитесь
4. Вы должны видеть строки вида: `X:2048,Y:2048,B:0`
5. При нажатии кнопок: `BTN:A`, `BTN:B` и т.д.

## Успешный запуск:

Если все работает:
- ✅ Курсор двигается при движении джойстика
- ✅ Рисование работает при удержании Button E
- ✅ Кнопки A-F выполняют свои функции
- ✅ В терминале нет ошибок

**Готово! Теперь вы можете рисовать джойстиком! 🎨**



