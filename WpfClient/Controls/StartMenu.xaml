<UserControl x:Class="WpfClient.Controls.StartMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <!-- Одна адаптивная панель-«карточка» по центру -->
        <Border Margin="32"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Style="{StaticResource CardBorderStyle}"
                Opacity="0.97">
            <StackPanel>
                <!-- Заголовок и подзаголовок -->
                <StackPanel Orientation="Horizontal">
                    <Border Width="38"
                            Height="38"
                            CornerRadius="12"
                            Background="{StaticResource AccentBrush}"
                            VerticalAlignment="Top"
                            Margin="0,0,12,0">
                        <TextBlock Text="🎨"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   FontSize="18" />
                    </Border>

                    <StackPanel>
                        <TextBlock Text="Paint Receiver"
                                   FontSize="30"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="Рисование с джойстиком и сохранением сессий"
                                   FontSize="15"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="Выберите режим и начните раскрашивать. Панели, управление и сессии будут сохранены — просто нажмите на режим, который подходит именно вам."
                           FontSize="14"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="0,16,0,0" />

                <!-- Фичи приложения -->
                <UniformGrid Columns="2"
                             Rows="2"
                             Margin="0,24,0,0"
                             HorizontalAlignment="Stretch">
                    <Border Style="{StaticResource PanelBorderStyle}" Margin="0,0,10,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFF3F5FF" Offset="0" />
                                <GradientStop Color="#FFE0E7FF" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="Режимы рисования"
                                       Style="{StaticResource PanelSectionHeaderTextStyle}" />
                            <TextBlock Text="Абсолютный и центрированный джойстик — выберите комфортный контроль курсора."
                                       TextWrapping="Wrap"
                                       Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PanelBorderStyle}" Margin="10,0,0,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFDF4E5" Offset="0" />
                                <GradientStop Color="#FFFDE7C6" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="Сохранение в БД"
                                       Style="{StaticResource PanelSectionHeaderTextStyle}" />
                            <TextBlock Text="Записывайте и пересматривайте сессии, отслеживайте прогресс и эксперименты."
                                       TextWrapping="Wrap"
                                       Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PanelBorderStyle}" Margin="0,10,10,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFE5FFF7" Offset="0" />
                                <GradientStop Color="#FFCCF5E7" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="Цветовая палитра"
                                       Style="{StaticResource PanelSectionHeaderTextStyle}" />
                            <TextBlock Text="Готовые пресеты и аккуратные заливки для быстрой работы с цветом."
                                       TextWrapping="Wrap"
                                       Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PanelBorderStyle}" Margin="10,10,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFF0EBFF" Offset="0" />
                                <GradientStop Color="#FFE1D4FF" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="Подсказки по управлению"
                                       Style="{StaticResource PanelSectionHeaderTextStyle}" />
                            <TextBlock Text="Горячие клавиши и советы собраны в одном месте, чтобы не отвлекаться от рисунка."
                                       TextWrapping="Wrap"
                                       Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Кнопки действий -->
                <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                    <Button Style="{StaticResource ElevatedButtonStyle}"
                            Background="{StaticResource AccentBrush}"
                            Padding="20,10"
                            Click="OnStartClick"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Начать рисовать" FontSize="14" />
                            <TextBlock Text="→" FontSize="16" Margin="8,0,0,0" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ElevatedButtonStyle}"
                            Background="#FF7043"
                            Padding="20,10"
                            Click="OnStartTimedClick"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Игра на время" FontSize="14" />
                            <TextBlock Text="⏱" FontSize="16" Margin="8,0,0,0" />
                        </StackPanel>
                    </Button>

                    <Border Background="#EFF3FB"
                            Padding="12,8"
                            CornerRadius="10"
                            VerticalAlignment="Center">
                        <TextBlock Text="В любой момент можно вернуться в меню — прогресс сессии сохранится."
                                   Foreground="{StaticResource MutedTextBrush}"
                                   FontSize="12" />
                    </Border>
                </StackPanel>

                <!-- Разделитель внутри одной панели -->
                <Separator Margin="0,28,0,20"
                           Opacity="0.4" />

                <!-- Краткий обзор холста как часть той же карточки -->
                <StackPanel>
                    <TextBlock Text="Что доступно на холсте"
                               FontSize="18"
                               FontWeight="SemiBold" />

                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                        <Ellipse Width="14" Height="14" Fill="{StaticResource AccentBrush}" />
                        <TextBlock Text="Контур и образец рисунка рядом — удобно сравнивать и заполнять."
                                   FontSize="14"
                                   Margin="12,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Ellipse Width="14" Height="14" Fill="#9C27B0" />
                        <TextBlock Text="Палитра и режимы джойстика справа, всегда под рукой."
                                   FontSize="14"
                                   Margin="12,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Ellipse Width="14" Height="14" Fill="#00BCD4" />
                        <TextBlock Text="Подсказки и горячие клавиши слева, чтобы не искать их в справке."
                                   FontSize="14"
                                   Margin="12,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Ellipse Width="14" Height="14" Fill="#4CAF50" />
                        <TextBlock Text="Запись и воспроизведение сессий внизу — идеальны для анализа и обучения."
                                   FontSize="14"
                                   Margin="12,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
